// config.js
const PHARUS_CONFIG = {
    // El código estático que el jugador deberá ingresar en la PC de Unity
    token: "PHARUS-T13RRA",
    
    // Tiempo que tarda en "evaluar" el perfil (en milisegundos)
    tiempoCargaMS: 3000,
    
    // Cantidad de clics en el título para romper la página
    clicsParaCorrupcion: 5,
    imagenes: {
        logoNormal: "logo.png",
        logoCorrupto: "logo_corrupto.png" // Archivo que mostrará al romperse la web
    },
    
    // Textos que cambian durante el evento transmedia
    textos: {
        easterEggLogo: "N O  H A Y  S A L I D A",
        easterEggBoton: "ASIMILARSE"
    }
};